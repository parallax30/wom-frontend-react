import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    body1: {
      textAlign: "justify",
    },
    body2: {
      textAlign: "justify",
    },
  },
  palette: {
    primary: {
      main: '#E92070',
    },
  },
});

export default theme;
